@* <div class="fiche" style="background-color: @Color;" @onclick="Flip"/> *@
@* <div class="fiche @(isFlipped ? "flip" : "")" style="background-color: @(Color == Colors.Red ? "red" : Color == Colors.Blue ? "blue" : "white");" @onclick="Flip" /> *@
<div class="fiche"
     style="background-color: @(Color == Colors.Red ? "red" : Color == Colors.Blue ? "blue" : "white")"
     @onclick="OnClick" />





@code {
    [Parameter]
    public Colors Color { get; set; }

    [Parameter]
    public EventCallback Flipped { get; set; }

    private async Task OnClick()
    {
        Console.WriteLine("Fiche clicked");
        await Flipped.InvokeAsync();
    }

}


